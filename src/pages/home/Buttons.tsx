import React from "react";
import Button from "@mui/material/Button";

export const Buttons: React.FC = () => {
  return (<Button variant="contained" sx={{backgroundColor: "#2CA58D"}}>View List</Button>);
};
